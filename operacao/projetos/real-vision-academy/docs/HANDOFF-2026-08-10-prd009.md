---
id: HANDOFF-2026-08-10-prd009
title: Handoff — Fases B e C do PRD-008 aprovadas, próximo é o PRD-009 (trilha)
type: handoff
project: real-vision-academy
created: 2026-08-10
updated: 2026-08-10
status: pronto para a próxima sessão
related:
  - PRD-008-leitor-narrado-design
  - PRD-009-trilha-gamificada
  - DECISIONS
  - TIMELINE
  - HANDOFF-2026-08-10-leitor-narrado
  - HANDOFF-2026-08-10-fase-b-teste
---

# Handoff — leitor narrado fechado, próximo é a trilha

> Terceiro handoff desta sequência do dia 10/08/2026. Os dois anteriores
> ([[HANDOFF-2026-08-10-leitor-narrado]], [[HANDOFF-2026-08-10-fase-b-teste]]) documentam o caminho até
> aqui. **Superado por [[HANDOFF-2026-08-12-bloco5-fechado]]** — o Bloco 5 mencionado como pendência
> abaixo já fechou.

## Onde estamos, em três linhas

O leitor de aula narrada está **completo e aprovado** — Blocos 0 a 4, Fases A, A-2, B e C do
[[PRD-008-leitor-narrado-design]] todas no ar e testadas pelo Felipe em aparelho real. `main` publicado
em **`f4dca32`**. A próxima coisa a fazer é o [[PRD-009-trilha-gamificada]], especificado e com **zero
código escrito**.

## O que aconteceu hoje, resumido

Handoff da sessão anterior dizia "Fase B especificada, não iniciada". Nesta sessão:

1. **Fase B** codada (materiais em acordeão, cartão da aula, nav sem rolagem horizontal) e publicada.
2. Felipe testou no celular e voltou com uma lista de 7 pedidos em áudio — pedi pra ele confirmar 3
   decisões ambíguas antes de tocar em código (uma delas desfazia uma decisão do mesmo dia).
3. **Fase C** codada (nav com Cursos/Materiais, duplo clique na aula, cartão redesenhado, materiais do
   player em acordeão, painel sem navegar-por-frase + volume arrastável + popup de cor, frase não esconde
   atrás do menu, título em letreiro) e publicada.
4. **Felipe testou de novo e aprovou:** "tudo está perfeito, deu tudo certo."

Decisões novas: D-044 a D-050 em [[DECISIONS]]. Narrativa completa em [[TIMELINE]].

## Estado do repositório

| Item | Valor |
|---|---|
| Repo | `operacao/projetos/_RV-Internos/sites/real-vision-site` (canônico) |
| `main` publicado | `f4dca32` |
| Pendências não comitadas de antes | `docs/academy/README.md` e `docs/seo-internacional/STATUS.md` — não são minhas, não toquei |

**Antes de mexer:** `git fetch` e comparar — `git status` sozinho usa cache.

## Armadilha nova descoberta hoje — leia antes de tentar verificar deploy sozinho

A Vercel deste projeto bloqueia `curl` e Playwright headless partindo deste ambiente com um "Vercel
Security Checkpoint" (HTTP 403). **Não é bug, não adianta insistir** — testado em duas rodadas hoje.
Depois de publicar, pedir pro Felipe confirmar abrindo no celular. Detalhe completo na skill `rv-academy`.

## O que fazer na próxima sessão: PRD-009, Bloco 1

O [[PRD-009-trilha-gamificada]] já tem tudo decidido (D-040 a D-042) e 4 blocos definidos:

1. **Bloco 1 — Rotas.** O leitor de tela cheia muda de `/academy/curso/:slug/aula/:lessonId` pra
   `/academy/curso/:slug/aula/:lessonId/ler`. A rota antiga passa a ser a **tela da aula** (nova). Três
   links internos precisam do `/ler`: `CoursePage.tsx:122`, `NarratedLessonPage.tsx:74` e `:113`
   (números de linha do PRD podem ter deslocado com as mudanças de hoje — conferir antes de editar).
2. **Bloco 2 — Tela da aula.** Título + materiais em acordeão (reusar o padrão que já existe em DOIS
   lugares agora: `MaterialsList.tsx` da tela do curso e `MaterialsPanel.tsx` de dentro do leitor) +
   botão que abre o leitor.
3. **Bloco 3 — A trilha.** Substitui a `CoursePage` — módulos em acordeão (mesmo padrão do
   `CourseSyllabus.tsx`), bolinha por aula, cor por estado (concluída/atual/não concluída), navegação
   livre (nada travado).
4. **Bloco 4 — Verificação final e publicação.**

Um bloco por vez, com aval do Felipe entre eles, mesmo critério de sempre (D-039): build limpo,
Playwright verde, print 390px revisado a olho, confirmar que o `main` publicado está no ar antes de pedir
teste real.

## Como rodar os testes

```bash
cd "operacao/projetos/_RV-Internos/sites/real-vision-site"
npm run dev
```

Perfil de **aluno** (o padrão dos scripts é admin, que não está matriculado):

```bash
RV_PROFILE="C:\Users\Felipe Garcia\.playwright-rv-aluno" node tests/verify-bloco1.mjs
```

Se "Marcadores" testar sujo (estado vazio falha), rodar `node tests/limpar-marcadores.mjs` primeiro —
utilidade nova criada hoje, limpa marcadores de teste pela interface.

## Documentos relacionados
- [[PRD-009-trilha-gamificada]] — o que fazer a seguir, já especificado
- [[PRD-008-leitor-narrado-design]] — leitor completo, referência de padrões já aprovados
- [[DECISIONS]] — D-044 a D-050 são de hoje
- [[TIMELINE]] — narrativa completa do dia
- [[HANDOFF-2026-08-10-leitor-narrado]] · [[HANDOFF-2026-08-10-fase-b-teste]] — handoffs anteriores desta
  mesma data, com contexto de conta de teste e armadilhas gerais
